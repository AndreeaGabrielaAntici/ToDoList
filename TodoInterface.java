import java.util.Scanner;

public class Todointerface {
    private string username;
    private string create ToDoList;

    Scanner input = new Scanner(System.in);TodoFeat=new TodoFeat();

    public void todo() {
        System.out.println("ToDoList");;
        System.out.println(Enter your first name);
        features.setUserName(username);
        System.out.println("Welcome" + features.getUserName() + "Click enter to continue");
        input.nextLine();
        System.out.println("Press enter to save");
        features.setToDoList(createTodos);
        features.createTxtFileForTodos();
    }
}
